/*

*/
//#include <stdio.h>
//
//int main()
//{
//    int ary[] = { 1, 2, 3 };        // 정수 1개당 4byte 총 12byte 배열 생성
//
//    printf("배열의 전체크기: %d\n", sizeof(ary));
//    printf("배열 방한개의 크기: %d\n", sizeof(ary[0]));             // 자료형이 같기에 아무 인덱스나 입력해도 됨
//    printf("배열 방의 갯수: %d\n", sizeof(ary) / sizeof(ary[0]));   // 방 1개의 갯수
//    printf("배열의 이름: %p\n", ary);                    // 배열의 시작주소 (배열의 첫번째 주소랑 같음)
//    printf("배열 첫 번째방 주소: %p\n", &ary[0]);        // 배열의 첫번째 주소 (& 주소연산자를 붙여줘야함)
//    printf("배열 두 번째방 주소: %p\n", ary + 1);        // 배열의 두번째 주소
//    printf("배열 두 번째방 주소: %p\n", &ary[1]);
//
//    return 0;
//}
